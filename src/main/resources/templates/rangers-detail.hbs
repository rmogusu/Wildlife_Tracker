{{#partial "content"}}
    <div class ="cards">
        <table class="table table-bordered">
            <thead>
            <tr>
                <th scope="col"> Ranger's Name:</th>
                <th scope="col"> Badge_No:</th>
                <th scope="col"> Contact:</th>
            </tr>
            </thead>
            <tbody>
            <tr>
            {{#if rangers}}
                {{#each rangers}}
                        <td>{{ name }}</td>
                        <td>{{ badgeNo }}</td>
                        <td>{{ contact }}</td>
                    </tr>
                {{/each}}
            {{else}}
                <p>No Rangers Recorded yet !</p>
            {{/if}}
            <p>current Rangers :</p>
            </tbody>
        </table>

        <a href="/rangers/delete"><button style="font-weight:10px;" name="submit" type="submit" id="sub" >DELETE All Rangers</button></a>
        <a href="/"><button style="font-weight:10px;" name="submit" type="submit" id="sub" >Back!</button></a>

    </div>
{{/partial}}

{{> layout.hbs}}